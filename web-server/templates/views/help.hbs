{{!-- <!DOCTYPE html>
<html>
    <head>

    </head>
    <title>{{ title }}</title>
    <body>
        <h1>{{ title }}</h1>
        <p>{{ message }}</p>
    </body>
</html> --}}
{{>header}}

   <p>{{ message }}</p>

{{>footer}}